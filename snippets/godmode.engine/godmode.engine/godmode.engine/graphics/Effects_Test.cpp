#ifdef test$
#include <godmode.foundation/test/Test.h>

// kamimodo™ - the legacy of GODMODE_999 © 2025 ____l____e____x____e____r____

#include <godmode.engine/graphics/Effects.h>

test$class(Effects, // TODO(4) - tests
    test$method(serialize) {
        /*Effects *effects = new Effects();
        Effects* effects2;

        ((Sharpen*)effects->registry.filter([](Effect* effect){ return effect->name == "sharpen"; }).at(0))->setIntensity(777);
        ((HighDynamicRange*)effects->registry.filter([](Effect* effect) { return effect->name == "high-dynamic-range"; }).at(0))->setBaseIntensity(11111);

        // FIXME: class boost::archive::archive_exception: unregistered class
        Serializer::pipe(effects, effects2);
        expect(effects->registry.length() == effects2->effects.length());

        delete effects;
        delete effects2;*/
    }
)
#endif